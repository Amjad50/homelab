# TikTok serves its HEVC (bytevc1) formats as video-only payloads, so yt-dlp's
# default vcodec preference picks one and -x audio extraction fails with
# "unable to obtain file audio codec with ffprobe". The h264 formats are muxed
# (audio+video), so prefer them.
-S vcodec:h264
